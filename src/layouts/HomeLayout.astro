---
import SkipToContent from '../components/Base/SkipToContent.astro';
import '../css/config.css'
import Footer from '../components/Base/Footer.astro';
import MainContent from '../components/Base/MainContent.astro';
import Meta from '../components/Base/Meta.astro';
import type { Layout } from './types';
import Header from '../components/Base/Header.astro';
import CallToActionBanner from '../components/Base/CallToActionBanner.astro';
import Hero from '../components/Base/Hero.astro';
import BackToTop from '../components/Base/BackToTop.astro';


interface Props extends Layout { }

const {
    title,
    description,
	lang,
	class : className
} = Astro.props
---

<html lang={lang}>

	<Meta title={title} description={description} />

	<body class:list={['bg-[var(--body-bg-color)] text-[var(--body-text-color)] font-base', className]}>

		<SkipToContent />

		<Header variant='fixed' props />
		
		<Hero />
		
        <MainContent>

            <slot />

        </MainContent>

		<CallToActionBanner />

        <Footer />

		<BackToTop />

		<slot name="pageEnd" />
		
	</body>
</html>


